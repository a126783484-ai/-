import { AppShell } from "@/components/AppShell";
import { workspace } from "@/lib/seed";

export default function SettingsPage() { const settings = ["店鋪名稱、電話、地址", "營業時間", "預約規則與取消政策", "付款方式", "服務分類", "稅務 / 收據設定", "品牌色與外觀設定"]; return <AppShell title="設定頁" subtitle="店鋪基本資料、預約規則、收款、稅務、品牌外觀與多店設定。"><div className="card p-5"><div className="grid gap-4 md:grid-cols-2"><label className="block text-sm font-semibold text-plum">店鋪名稱<input className="mt-2 w-full rounded-2xl border border-champagne p-3" defaultValue={workspace.name}/></label><label className="block text-sm font-semibold text-plum">電話<input className="mt-2 w-full rounded-2xl border border-champagne p-3" defaultValue={workspace.phone}/></label><label className="block text-sm font-semibold text-plum md:col-span-2">地址<input className="mt-2 w-full rounded-2xl border border-champagne p-3" defaultValue={workspace.address}/></label></div><div className="mt-6 grid gap-3">{settings.map((item) => <div key={item} className="rounded-2xl bg-blush p-4 font-medium">{item}</div>)}</div><button className="mobile-tap mt-5 rounded-2xl bg-plum font-semibold text-white">儲存設定</button></div></AppShell>; }
