{
  "version": 1,
  "steps": [
    {
      "name": "Check if component is being used correctly",
      "description": "Check if the component is being used correctly",
      "type": "check",
      "params": {
        "component": "OperationsPage"
      }
    },
    {
      "name": "Check if component is being rendered correctly",
      "description": "Check if the component is being rendered correctly",
      "type": "check",
      "params": {
        "component": "OperationsPage"
      }
    },
    {
      "name": "Check if component is handling errors correctly",
      "description": "Check if the component is handling errors correctly",
      "type": "check",
      "params": {
        "component": "OperationsPage"
      }
    }
  ]
}