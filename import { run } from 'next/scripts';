export default async function ci() {
  try {
    await run('build');
    await run('test');
    await run('lint');
  } catch (error) {
    console.error('CI failed:', error);
    process.exit(1);
  }
}